{
  "name": "magic-functions",
  "description": "functions for magic",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "dev":
      "nodemon -e ts --exec \"npm run build && npm run start\" && npm run clean",
    "build": "npm run clean && tsc",
    "clean": "rimraf build build.zip"
  },
  "author": "cruhl",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:cruhl/magic.git"
  },
  "dependencies": {
    "aws-sdk": "^2.131.0",
    "axios": "^0.16.2",
    "source-map-support": "^0.5.0",
    "twilio": "^3.7.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "0.0.17",
    "@types/node": "^8.0.34",
    "nodemon": "^1.12.1",
    "rimraf": "^2.6.2"
  }
}
