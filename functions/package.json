{
  "name": "magic-functions",
  "description": "functions for magic",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon -e ts --exec \"npm run build && npm run start\" && npm run clean",
    "build": "tsc",
    "clean": "rimraf ./build/"
  },
  "author": "cruhl",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com:cruhl/magic.git"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "source-map-support": "^0.5.0",
    "twilio": "^3.7.0"
  },
  "devDependencies": {
    "nodemon": "^1.12.1",
    "rimraf": "^2.6.2"
  }
}
